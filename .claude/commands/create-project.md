---
description: "Create a new project in a domain. Guides through domain selection, goal, tasks, and priority. Saves to domains/[name]/02_PROJECTS/PROJECT_*.md."
---

Run the `create-project` workflow from the `project-management` skill.

$ARGUMENTS
