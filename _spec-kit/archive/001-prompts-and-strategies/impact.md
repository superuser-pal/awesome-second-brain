---
title: "Impact: Dormant Prompts & Strategies"
description: "Enumerated list of .claude/core/ files, commands, skills, hooks, bases, and indexes this feature will modify or create. Every edit in /spec-kit:build must map to a row below."
tags:
  - spec-kit
  - impact
feature: "001-prompts-and-strategies"
status: draft
date: "2026-04-16"
---

# Impact: Dormant Prompts & Strategies

> Related: [[plan]] · [[CONSTITUTION|Spec-Kit CONSTITUTION §4]]
>
> **Rule**: every edit the builder makes during `/spec-kit:build` must correspond to a row below. If a new impact is discovered mid-build, pause and amend this file.

## `.claude/core/` updates

| File | Section / change | Reason |
|---|---|---|
| `.claude/core/reference/ASSET-CLASSES.md` | Add "Prompt (Dormant or Active)" section with `type: prompt` schema | Register new asset class so `validate-write.ts` warns on missing frontmatter |
| `.claude/core/reference/ASSET-CLASSES.md` | Add "Strategy (Dormant or Active)" section with `type: strategy` schema | Same, for strategies |
| `.claude/core/reference/ROUTING-TABLE.md` | Add 4 rows: prompt→dormant-prompts, prompt→active-prompts, strategy→dormant-strategies, strategy→active-strategies | Tell `/core:distribute` where to route these types |
| `.claude/core/reference/SYSTEM-INDEX.md` | Add entries for `prompts` skill, `strategy` skill, `/thinking:reset`, `/thinking:eval`, `/thinking:cot` | Keep system index complete per CONSTITUTION §5 |

## Commands

| Path | Action (add/modify/remove) | Summary |
|---|---|---|
| `.claude/commands/thinking/reset.md` | add | Clears the currently-injected strategy from session context |
| `.claude/commands/thinking/eval.md` | add | Evaluates which active strategy best fits the user's stated goal (active-only, strict) |
| `.claude/commands/thinking/cot.md` | add | Chain-of-Thought stub — the one pre-promoted strategy at setup |
| `.claude/commands/prompts/<slug>.md` | add (dynamic, on promotion) | Stub generated by `prompts` skill's `promote` workflow. Zero stubs exist at setup — active-prompts/ starts empty |
| `.claude/commands/thinking/<slug>.md` | add (dynamic, on promotion) | Stub generated by `strategy` skill's `promote` workflow for strategies beyond `cot` |

## Skills

| Path | Action | Summary |
|---|---|---|
| `.claude/skills/prompts/SKILL.md` | add | Name `prompts`, user-invocable, routing table for promote/demote/search |
| `.claude/skills/prompts/workflows/promote.md` | add | `mv` dormant → active, flip status, generate command stub, `qmd refresh` |
| `.claude/skills/prompts/workflows/demote.md` | add | Reverse of promote: delete stub, `mv` active → dormant, flip status |
| `.claude/skills/prompts/workflows/search.md` | add | QMD semantic search scoped to both prompt folders |
| `.claude/skills/prompts/references/category-rules.md` | add | Verb-prefix → category mapping table |
| `.claude/skills/strategy/SKILL.md` | add | Name `strategy`, user-invocable, routing table for promote/demote/search |
| `.claude/skills/strategy/workflows/promote.md` | add | `mv` dormant → active, flip status, generate `/thinking:<slug>` stub, `qmd refresh` |
| `.claude/skills/strategy/workflows/demote.md` | add | Reverse of promote |
| `.claude/skills/strategy/workflows/search.md` | add | QMD semantic search scoped to both strategy folders |

## Subagents / Domain agents

| Path | Action | Summary |
|---|---|---|
| — | none | No subagent or domain-agent changes in this feature |

## Hooks

| Path | Action | Summary |
|---|---|---|
| `.claude/scripts/hooks/lib/schemas.ts` | modify | Add `promptSchema` + `strategySchema`; extend `resolveSchema()` with 4 path matchers (`docs/prompts/dormant-prompts/`, `docs/prompts/active-prompts/`, `docs/strategies/dormant-strategies/`, `docs/strategies/active-strategies/`). Data change only — no behavioral code changes |
| `.claude/scripts/hooks/classify-message.ts` | **NOT TOUCHED** | Explicit non-change per user directive |
| `.claude/scripts/hooks/lib/signals.ts` | **NOT TOUCHED** | Explicit non-change per user directive |
| `.claude/scripts/hooks/validate-write.ts` | **NOT TOUCHED** | Picks up new schemas via `resolveSchema()` automatically |

## Templates

| Path | Action | Summary |
|---|---|---|
| — | none | Command-stub template is embedded as a heredoc inside each skill's `promote` workflow; no shared template file added |

## Frontmatter schemas (`ASSET-CLASSES.md`)

| Asset class | Field(s) | Change |
|---|---|---|
| prompt (new) | `type`, `status` (dormant\|active), `category`, `description`, `tags` | Added |
| strategy (new) | `type`, `status` (dormant\|active), `description`, `tags` | Added |

## Bases

| Path | Action | Summary |
|---|---|---|
| `bases/Prompts.base` | add | Views: Active, Dormant by category, All. Filters: `file.inFolder("docs/dormant-prompts")` OR `file.inFolder("docs/active-prompts")` |
| `bases/Strategies.base` | add | Views: Active, Dormant, All. Filters: `file.inFolder("docs/dormant-strategies")` OR `file.inFolder("docs/active-strategies")` |

## Content (one-time placement, Phase 3 Task 8)

| Path | Action | Summary |
|---|---|---|
| `docs/prompts/dormant-prompts/INDEX.md` | add | MOC grouping by category + status |
| `docs/prompts/dormant-prompts/<category>/<slug>.md` | add (~240 files) | Pattern bodies with generated `type: prompt` frontmatter |
| `docs/prompts/active-prompts/INDEX.md` | add | MOC of currently-promoted prompts — empty at setup |
| `docs/strategies/dormant-strategies/INDEX.md` | add | MOC of dormant strategies — 8 entries at setup |
| `docs/strategies/dormant-strategies/<slug>.md` | add (8 files) | The 8 non-`cot` strategy texts with `type: strategy`, `status: dormant` |
| `docs/strategies/active-strategies/INDEX.md` | add | MOC of active strategies — 1 entry at setup |
| `docs/strategies/active-strategies/cot.md` | add | Chain-of-Thought body with `status: active` |

## Spec-kit feature files

| Path | Action | Summary |
|---|---|---|
| `_spec-kit/features/001-prompts-and-strategies/spec.md` | modify (Task 0) | Rewrite to reflect `docs/` paths, drop hook auto-suggestion, add `dormant-strategies`/`active-strategies`, add `/thinking:eval` + `/thinking:reset`, remove all source-framework references |
| `_spec-kit/features/001-prompts-and-strategies/tasks.md` | add (by `/spec-kit:build`) | Generated from plan + impact; not edited by hand |

## Indexes touched

- [x] `.claude/CLAUDE.md` — add "Prompts & Strategies" section; register `prompts` and `strategy` in Skills list
- [ ] `.claude/core/system/WORKFLOWS.md` — **unclear if this file exists; builder to verify in Phase 3 and append row only if present**
- [ ] `.claude/core/system/SKILL-LOGIC.md` — add `prompts` and `strategy` to the skills registry if the file maintains one
- [ ] `.claude/core/system/AGENTS-LOGIC.md` — **no change** (no agent work in this feature)
- [x] `.claude/core/reference/SYSTEM-INDEX.md` — add new skills + commands
- [x] `.claude/core/reference/ROUTING-TABLE.md` — add 4 rows
- [x] `brain/SKILLS.md` — register `prompts` and `strategy`
- [x] `CHANGELOG.md` — v-next entry
- [x] `vault-manifest.json` — bump version; register 4 new `docs/` subfolders as infrastructure paths
